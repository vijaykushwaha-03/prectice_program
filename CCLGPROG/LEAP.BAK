#include<stdio.h>
#include<conio.h>

void main()
{
       //	int a=10,b=20,c=30;
	int yr;

	clrscr();

	printf("\n\n\t Enter year to check leap year or not =>");
	scanf("%d",&yr);
      //	(a>b && a>c) ? ( b>a && b>c) ? printf(" a is biiger %d",a) : printf(" b is bigger %d",b) : printf(" c is bigger %d",c);

	if( yr%100 == 0 &&  yr%400 == 0 )
	{
		printf("\n\n\t %d is leap year ",yr);
	}
	else if ( yr%100 != 0  &&  yr%4 == 0 )
	{
		printf("\n\n\t %d is leap year ",yr);
	}
	else
	{
		printf("\n\n\t %d is not leap year",yr);

	}
	getch();
	}